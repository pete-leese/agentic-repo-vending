# {{REPO_NAME}}

{{DESCRIPTION}}

## Origin

- **Jira:** `{{ISSUE_KEY}}`
- **Summary:** {{SUMMARY}}
- **Created from template:** `{{TEMPLATE}}`
- **Repository:** {{REPO_URL}}

## Layout

- Root `.tf` files — Terraform configuration for this repo (no nested `modules/` tree)
- `.github/workflows/terraform.yml` — fmt, validate, tflint
- `.github/workflows/terraform-docs.yml` — regenerates docs below and lands on `main`
- `.pre-commit-config.yaml` — fmt, validate, tflint, terraform-docs
- `.terraform-docs.yml` — terraform-docs inject config

## Getting started

```bash
terraform init
terraform plan
```

## Terraform docs

<!-- BEGIN_TF_DOCS -->
Placeholder — regenerated by terraform-docs (pre-commit / GitHub Actions) from `.tf` in this repo.
<!-- END_TF_DOCS -->

## Contributing

Direct pushes to `main` should be blocked — open a pull request. Terraform docs updates are opened as a PR by GitHub Actions and merged back to `main`.
