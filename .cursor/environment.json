{
  "install": "curl -LsSf https://astral.sh/uv/install.sh | sh && export PATH=\"$HOME/.local/bin:$PATH\" && uv sync --group dev --extra cursor --extra otel || uv sync --group dev --extra otel || uv sync --group dev",
  "terminals": []
}
